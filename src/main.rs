mod staffmatcher;
mod linedetect;

fn main() {
    println!("Hello world!");
/*
    let image = image::io::Reader::open("path/to/image.png")?
        .decode()?;

    let test = image.as_luma8();
 */
//Récupérer toutes les images du dossier
//Pour chaque image lancer le scan sur la totalité, une tranche, 2 tranches
//Logger toutes les informations

}
